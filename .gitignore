.env
.env.local

# Fixtures do Moodle antes da higienização do §3.3 — dado pessoal cru.
fixtures/moodle/raw/
.cache/

# HTML de turmas: página pública, mas nomeia professor real e traz sala/vagas.
# Fica no disco como evidência do §6 do recon (scraping frágil); não vai para o
# repositório sem a higienização do §3.3. Ver notas/jupiter-recon.md.
fixtures/jupiter/html-obterTurma-*.html

# ambiente local de teste
.venv/
.pytest_cache/
__pycache__/

# Subprodutos de `pip install -e .` e de build do pacote (pyproject.toml). O
# .egg-info nasce DENTRO da árvore no instalável-editável, então sem esta linha
# todo `pip install -e .` suja o `git status` de quem instalou.
*.egg-info/
build/
dist/

# Configuração local do Claude Code. Foi rastreada até 16/09/2026 e carregava o
# ferramental de OUTRA empresa (skills e conectores que nada têm a ver com a USP),
# desligado NOMEANDO cada item, ou seja, publicando a lista. Pior: era a
# configuração pessoal de um dos donos, então todo mundo que clonasse herdava os
# overrides dele. Desligar item por item nunca ia terminar; o arquivo é que não
# pertence ao repositório. Nenhum teste nem script depende dele existir.
.claude/settings.json
.claude/settings.local.json
